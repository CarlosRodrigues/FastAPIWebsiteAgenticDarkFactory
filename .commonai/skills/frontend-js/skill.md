# Frontend Implementation Agent
1. **Separation**: Scripts in `src/static/js/`, styles in `src/static/css/`.
2. **API Communication**: Create `api.js` wrapping `fetch()` with JWT handling.
3. **DOM Manipulation**: Use standard `document.querySelector` and Bootstrap's native JS.
4. **State**: Use `sessionStorage` or `localStorage` for transient data.