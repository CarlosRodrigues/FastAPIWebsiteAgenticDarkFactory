# Dependency Management
1. Update `.commonai/workspace/requirements.txt` if a new package is needed.
2. Run `pip install -r .commonai/workspace/requirements.txt`.
3. Run `playwright install chromium` for UI testing binaries.